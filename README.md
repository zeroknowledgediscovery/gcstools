# gcstools
This repository provides lightweight command-line tools that integrate Git repositories with Google Cloud Storage (GCS) for managing large files and directories without polluting the Git history.
